services:

  # main service
  mybooks:
    restart: always
    image: poxenstudio/mybooks
    volumes:
      - /tmp/mybooks:/data
    ports:
      - "3000:80"
      - "3443:443"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Shanghai
    depends_on:
      - douban-rs-api

  # optional, for meta plugins
  # please set "http://douban-rs-api" in settings
  douban-rs-api:
    restart: always
    image: poxenstudio/douban-api-rs
